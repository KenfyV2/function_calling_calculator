name: Python application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        
    - name: Install dependencies
      run: |
        python3 -m venv venv
        source venv/bin/activate
        pip install --upgrade pip
        pip install -r requirements.txt
<<<<<<< HEAD
        playwright install
        
    - name: Run tests
      run: |
        source venv/bin/activate
        pytest tests/
=======

    - name: Run tests
      run: |
        source venv/bin/activate
        pytest tests/
>>>>>>> f4b56d5 (Add bcrypt to requirements and update GitHub Actions workflow)
